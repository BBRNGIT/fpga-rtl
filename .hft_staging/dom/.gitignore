# build-sequence law (corrected): THIS device's generated logic IS COMMITTED and
# graduated. dom_gen.h is NOT ignored — it is tracked + checksummed against
# dom.net.json (the netlist is the validation spec for the committed circuit).
#
# The SIBLING headers below are regenerated copies of OTHER devices' single-source
# circuits (the fifo_rx head this DOM samples + the nic/wire/tai_cdc the FIFO pulls
# in). They are NOT this device's deliverable — ignore them (regenerated by `make`).
fifo_rx_gen.h
nic_gen.h
wire_gen.h
tai_cdc_gen.h
# compiled artifacts
*.bin
test_synth
probe
*.dSYM/
