# build-sequence law (corrected): THIS device's generated logic IS COMMITTED and
# graduated. fifo_rx_gen.h is NOT ignored — it is tracked + checksummed against
# fifo_rx.net.json (the netlist is the validation spec for the committed circuit).
#
# The SIBLING headers below are regenerated copies of OTHER devices' single-source
# circuits (the nic seam this FIFO samples + the wire/tai_cdc the nic pulls in).
# They are NOT this device's deliverable — ignore them (regenerated by `make`).
nic_gen.h
wire_gen.h
tai_cdc_gen.h
# compiled artifacts
*.bin
test_synth
probe
*.dSYM/
